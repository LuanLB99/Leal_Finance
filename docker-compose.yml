services:
  db:
    image: postgres:15
    container_name: financas-db
    environment:
      - POSTGRES_USER=USER
      - POSTGRES_PASSWORD=PASSWORD
      - POSTGRES_DB=MYDB
    ports:
      - "5432:5432"